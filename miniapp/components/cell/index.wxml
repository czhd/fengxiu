<!--components/cell/index.wxml-->
<wxs src="../../wxs/cell.wxs" module="c"></wxs>
<view bind:tap="onTap" class="container {{c.cellStatus(cell.status).outer}}">
	<view class="inner-container {{c.cellStatus(cell.status).inner}}">
		<text>{{cell.title}}</text>
	</view>
</view>
